# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# 서명 키 — 절대 커밋 금지. CI(release.yml)가 Secrets 에서 이 경로로 생성하므로
# 로컬에 남아 있어도 스테이징되지 않도록 막아 둔다. 자세한 내용은 README '보안' 절 참고.
*.jks
*.keystore
key.properties
android/key.properties

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Native build artifacts
android/app/.cxx/
android/build/
android/.kotlin/
ios/build/
macos/build/

# Rust (native/mocl_parser) — 소스만 추적, 빌드 산출물 제외
native/**/target/

# FVM (Flutter Version Management)

# FVM Version Cache
.fvm/